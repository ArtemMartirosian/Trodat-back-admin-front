import {useClassName} from '../../utils/cn';
import './style.scss';

const NewsPage = () => {
    const cn = useClassName('');
    return (
        <div className={cn()}>

        </div>
    );
};

export default NewsPage;
